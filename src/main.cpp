#include "raylib.h"
#include "randomize.h"
#include "surface.h"
//------------------------------------------------------------------------------------
// Program main entry point
//------------------------------------------------------------------------------------
int main()
{/*
    Color background = {31,69,100,100};
    InitWindow(300,600,"raylib");
    SetTargetFPS(30);
    while(WindowShouldClose() == false){

        BeginDrawing();
        ClearBackground(background);
        EndDrawing();

    //Event Handling

    //Updating Positions 

    //Drawing Objects

    }
    CloseWindow();
    */
    Surface surf;
    return 0;
}
